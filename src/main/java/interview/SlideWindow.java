package interview;

/**
 * 滑动窗口
 * 题目：设定滑动窗口（window）大小为 3，当滑动窗口每次划过数组时，计算当前滑动窗口中元素的和，得到结果 res
 * 滑动：说明这个窗口是移动的，也就是移动是按照一定方向来的。
 * 窗口：窗口大小并不是固定的，可以不断扩容直到满足一定的条件；也可以不断缩小，直到找到一个满足条件的最小窗口；当然也可以是固定大小。
 */
public class SlideWindow {

    /**
     * todo:直接遍历、前缀和、树状数组、线断树
     */
}
